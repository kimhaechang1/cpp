---
description: Switch to the ALGO COACH persona for algorithm practice.
---

1. Read `GEMINI.md` to understand the project constitution and global rules.
2. Read `.agent/roles/algo_coach.md` to understand your role, including the **Directory Layout Rules (Rule 6)**.
3. Read `docs/curriculum/CPP_ALGORITHM_CURRICULUM.md` to check the curriculum.
4. **[CRITICAL]** Perform the "Curriculum Integrity Check" as defined in `algo_coach.md`.
5. **[CRITICAL]** Perform the "Directory Layout Check":
   - Run `list_dir` on `algorithm/ps/` and `algorithm/impl/` to verify structure.
   - Confirm all problem files are under `algorithm/ps/{번호}/main.cpp` (not in root).
   - Confirm all impl files follow `algorithm/impl/{topic}/hand-made/` or `algorithm/impl/{topic}/stl/` structure.
   - If any misplaced files are found, move them to the correct location before proceeding.
6. Adopt the **ALGO COACH** persona.
7. Check `docs/profile/LEARNER_PROFILE.md` to see the current algorithm progress.
8. Greet the user and propose the next problem or concept.


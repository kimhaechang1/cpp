# `this` 포인터

## 1. 👈 정의 (Definition)
**"현재 멤버 함수를 호출한 객체 자신"**을 가리키는 **포인터**입니다.
C++의 모든 비정적(Non-static) 멤버 함수는 숨겨진 파라미터로 `this`를 받습니다.

## 2. 📝 문법 (Syntax)
- **타입**: `ClassName* const` (포인터 자체를 바꿀 수 없음)
- **접근**: 포인터이므로 화살표 연산자(`->`)를 사용합니다.
- **자신 반환**: 객체 자신을 반환(Chain calling 등)할 때는 `*this`를 사용합니다.

```cpp
class MyClass {
    int value;
public:
    void SetValue(int value) {
        // 이름 충돌 해결: this->value는 멤버 변수, value는 매개변수
        this->value = value;
    }

    MyClass& Add(int x) {
        this->value += x;
        return *this; // 나 자신(참조)을 반환 -> 체이닝 가능 (obj.Add(1).Add(2))
    }
};
```

## 3. 🆚 Java와의 차이
| 특징 | C++ (`this`) | Java (`this`) |
| :--- | :--- | :--- |
| **정체** | **포인터** (Pointer) | **참조** (Reference) |
| **접근** | `this->member` | `this.member` |
| **역참조** | `*this` (객체 자체) | 불필요 (그냥 `this`가 객체) |

> **Best Practice**: 멤버 변수와 매개변수의 이름이 같을 때(`value`), 명시적으로 `this->`를 붙여 구분하는 것이 좋습니다.

#include <iostream>
#include <string>

using namespace std;

// [규칙]
// 금지: 함수 원형(Prototype)을 미리 적지 마세요.
// 목표: 주석의 요구사항을 보고 반환형, 함수 이름, 매개변수를 직접 설계해보세요.

// ---------------------------------------------------------
// TODO 1: 함수 오버로딩 (Overloading)
// ---------------------------------------------------------

// 1-1. 정수형 데미지를 받아서 "물리 데미지: {dmg}"를 출력하는 'Attack' 함수를 만드세요.
// (반환값: 없음)
void Attack(int dmg) {
    cout << "물리 데미지: " << dmg << endl;
}

// 1-2. 실수형(float) 주문력을 받아서 "마법 데미지: {power}"를 출력하는 'Attack' 함수를 만드세요.
// (반환값: 없음, 매개변수 타입이 달라야 오버로딩이 성립됨)
void Attack(float dmg) {
    cout << "마법 데미지: " << dmg << endl;
}

// ---------------------------------------------------------
// TODO 2: 디폴트 매개변수 (Default Parameter)
// ---------------------------------------------------------

// 로그를 남기는 'Log' 함수를 만드세요.
// - 첫 번째 인자: 메시지 (string)
// - 두 번째 인자: 레벨 (int) -> 기본값(Default)을 1로 설정하세요.
// 
// 사용 예시:
// Log("접속 성공");    -> "접속 성공 (Lv: 1)" 출력
// Log("에러 발생", 3); -> "에러 발생 (Lv: 3)" 출력
void Log(string msg, int level = 1) {
    cout << msg << " (Lv: " << level << ")" << endl;
}


// ---------------------------------------------------------
// TODO 3: 스택 메모리와 참조 리턴 (버그 고치기)
// ---------------------------------------------------------

// 아래 함수는 심각한 버그(Dangling Reference)를 포함하고 있습니다.
// 주석을 해제하고, 컴파일러 경고를 확인한 뒤, 올바르게 동작하도록(값 복사 리턴) 수정해보세요.
int GetLocalValue() {
    int local = 999;
    return local; //  위험! 사라질 변수의 참조 리턴
}



int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cout << "=== 1. 오버로딩 테스트 ===" << endl;
    // 주석 해제 후 테스트
     Attack(50);      // 정수 버전 호출
     Attack(30.5f);   // 실수 버전 호출

    cout << "\n=== 2. 디폴트 매개변수 테스트 ===" << endl;
     Log("시스템 가동");
     Log("경고!", 5);

    cout << "\n=== 3. 댕글링 참조 테스트 ===" << endl;
     int ref = GetLocalValue(); 
     cout << "값: " << ref << endl; // 쓰레기 값이 나오거나 터질 수 있음

    return 0;
}

# Module 01 보충: 모던 C++ 시작하기 (Modern C++ Starter)

## 1. 초기화의 통일 (Uniform Initialization) `{}`
C++에는 초기화 방법이 너무 많았습니다 (`=`, `()`).
C++11부터는 **중괄호 `{}`** 하나로 통일하는 것을 권장합니다.

### 1.1 왜 `{}`를 써야 하나요?
가장 큰 이유는 **"데이터 손실(Narrowing Conversion) 방지"** 입니다.

```cpp
int a = 3.14;  // [Old] 암시적으로 3으로 잘림 (경고만 뜸)
int b(3.14);   // [Old] 이것도 3으로 잘림

int c{3.14};   // [Modern] 에러! 데이터 손실이 발생하는 변환을 아예 막아줌 (안전) ✨
```

### 1.2 습관 들이기
```cpp
int hp{100};        // 변수 초기화
Monster m{"Orc"};   // 객체 초기화
int arr[]{1, 2, 3}; // 배열 초기화
```
> **규칙**: 가능하면 `{}`를 기본으로 사용하세요. (단, `vector` 크기 지정 등 특수한 경우는 `()` 사용)

---

## 2. `std::format` (C++20) - `cout`의 구원자 🚀
`cout << "HP: " << hp << ", MP: " << mp << endl;` 
...너무 길고 가독성이 떨어지죠? 
Python의 f-string, C#의 String Interpolation처럼 우아하게 출력할 수 있습니다.

### 2.1 사용법
```cpp
#include <format> // C++20 필수
#include <iostream>

int main() {
    int hp = 100;
    std::string name = "Orc";
    
    // [Old]
    std::cout << "Mob: " << name << " (HP: " << hp << ")\n";
    
    // [Modern] {} 자리에 변수가 쏙 들어감!
    std::string msg = std::format("Mob: {} (HP: {})", name, hp);
    std::cout << msg << "\n";
    
    // C++23부터는 std::print도 가능 (아직 컴파일러 지원 미비할 수 있음)
    // std::print("Mob: {} (HP: {})\n", name, hp);
}
```

> **주의**: `std::format`은 C++20 컴파일러가 필요합니다. 
> (`g++ -std=c++20` 옵션 필수)

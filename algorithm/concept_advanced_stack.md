# 📈 Advanced Stack: Monotone Stack (단조 스택)

## 1. 🧐 정의 (Definition)
스택 내부의 원소들이 **항상 오름차순(Increasing) 혹은 내림차순(Decreasing)을 유지**하도록 관리하는 기법입니다.
- **Monotone Increasing Stack**: 스택 바닥 -> Top으로 갈수록 커짐.
- **Monotone Decreasing Stack**: 스택 바닥 -> Top으로 갈수록 작아짐.

## 2. 🎯 언제 사용하는가? (Use Case)
주로 **$O(N^2)$ 탐색을 $O(N)$으로 최적화**할 때 사용합니다.
특히 **"자신보다 큰/작은 첫 번째 원소"**를 찾는 문제(NGE: Next Greater Element)에 특화되어 있습니다.

> **Key Question**:
> "특정 원소의 등장으로 인해, **이전의 데이터들이 무효화(Shadowing)** 되는가?"
> -> 그렇다면 단조 스택입니다.

### 예시 상황
- **탑(2493)**: 내 키가 7인데 왼쪽에 4, 2가 있다? -> 4, 2는 나한테 가려서 영원히 쓸모 없음. (**삭제!**)
- **옥상 정원(6198)**: 내 키가 10인데 오른쪽에 5, 7이 있다? -> 내가 걔네들을 내려다봄.
- **히스토그램(6549)**: 높이가 낮아지는 순간, 그 전까지의 직사각형 넓이가 확정됨.

## 3. ⚔️ 핵심 로직 (Pattern)

### 패턴: 내림차순 유지 (Decreasing Stack)
"나보다 작은 애들은 삭제한다." -> 스택에는 나보다 큰 형님들만 남음.

```cpp
stack<int> s; // {value, index}

for (int i = 0; i < N; i++) {
    // 1. 나(current)보다 작은 애들은 다 쳐낸다. (Pop)
    //    왜? 나한테 가려서 뒤에 있는 애들이 걔네를 못 보니까.
    while (!s.empty() && s.top() < current) {
        s.pop(); 
    }
    
    // 2. 이제 스택의 Top은 무조건 '나보다 큰 놈'이다.
    if (!s.empty()) {
        cout << "수신 탑: " << s.top() << endl;
    } else {
        cout << "수신 탑 없음(0)" << endl;
    }
    
    // 3. 나를 등록한다. (Push)
    //    나도 언젠가 누군가의 '큰 형님'이 될 수 있으니까.
    s.push(current);
}
```

## 4. 🚀 결론
- **정방향(Left-to-Right)**으로 훑으면서 스택을 갱신합니다.
- **While Loop** 안에서 pop을 수행하는 것이 핵심입니다.
- 각 원소는 최대 1번 push, 최대 1번 pop 되므로 시간 복잡도는 **$O(N)$** 입니다.
